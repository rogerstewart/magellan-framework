# Introduction #

Full documentation coming soon. In short, use `ViewModelFactory` (or `IViewModelFactory` if you want IOC), and `ViewModelRouteCatalog`. `ViewModels` should have `Initialize` methods that take any route parameters as arguments.